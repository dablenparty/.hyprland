# Maintainer: Your Name <youremail@domain.com>
pkgname=Hyprland
pkgver=155eba57d
pkgrel=1
pkgdesc="A modern compositor, Hyprland provides the latest Wayland features, dynamic tiling, all the eyecandy, powerful plugins and much more, while still being lightweight and responsive"
arch=(x86_64)
url="https://github.com/hyprwm/Hyprland"
license=('GPL')
depends=(
  aquamarine-git
  blueman
  cairo
  cpio
  egl-wayland
  foot
  git
  hyprcursor-git
  hyprgraphics-git
  hypridle-git
  hyprland-qtutils-git
  hyprlang-git
  hyprlock-git
  hyprpolkitagent-git
  hyprshot-git
  hyprutils-git
  kitty
  libdisplay-info
  libinput
  libliftoff
  libx11
  libxcb
  libxcomposite
  libxcursor
  libxfixes
  libxkbcommon
  libxrender
  mako
  nwg-look
  obsidian
  pango
  perl-image-exiftool
  pixman
  python-pywal16
  qt5-wayland
  qt6-wayland
  re2
  rofi-wayland
  sddm
  seatd
  swww
  tesseract
  tomlplusplus
  ttf-font-awesome
  upscayl-ncnn
  uwsm
  waybar
  wayland-protocols
  waypaper
  wl-clipboard
  xcb-proto
  xcb-util
  xcb-util-errors
  xcb-util-keysyms
  xcb-util-wm
  xdg-desktop-portal-gtk-git
  xdg-desktop-portal-hyprland-git
  xorg-xwayland
  yazi
)
makedepends=(
  cmake
  hyprwayland-scanner-git
  gcc
  glaze
  make
  meson
  ninja
)
provides=("hyprland" "wayland-compositor")
conflicts=(hyprland)
source=("git+${url}.git")
sha256sums=(SKIP)

prepare() {
  cd "$srcdir/Hyprland" || exit 1
  git submodule update --init --remote --recursive --rebase
}

pkgver() {
  cd "$srcdir/Hyprland" || exit 1
  local commit_hash
  commit_hash=$(git rev-parse --short HEAD)
  echo "$commit_hash" | sed 's/[:\/\-]//g'
}

build() {
  cd "$srcdir/Hyprland" || exit 1
  make all
}

package() {
  cd "$srcdir/Hyprland" || exit 1
  make install DESTDIR="$pkgdir/"

  # license
  install -Dm0644 -t "$pkgdir/usr/share/licenses/$pkgname" LICENSE
}
